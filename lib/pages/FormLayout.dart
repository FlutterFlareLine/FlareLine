import 'package:flutter/material.dart';

class FormLayoutPage extends StatelessWidget {
  const FormLayoutPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('FormLayout'),
    );
  }
}